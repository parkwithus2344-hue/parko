[build]
  base    = "backend"
  command = "npm install"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# All /api/* calls → the serverless Express function
[[redirects]]
  from   = "/api/*"
  to     = "/.netlify/functions/api/:splat"
  status = 200
  force  = true
